{% if context.day.reminders %}
Reminders by status (meaning in parentheses):
{% set status_descriptions = {
  "INCOMPLETE": "Still needs attention.",
  "COMPLETE": "Already done.",
  "PUNT": "Intentionally deferred; do not notify, included only as context."
} %}
{% for status in ["INCOMPLETE", "COMPLETE", "PUNT"] %}
{% set items = context.day.reminders | selectattr("status.value", "equalto", status) | list %}
{% if items %}
{{ status }} ({{ status_descriptions[status] }}):
{% for reminder in items %}
{% include "components/reminders/item.j2" %}
{% endfor %}
{% endif %}
{% endfor %}
{% endif %}
